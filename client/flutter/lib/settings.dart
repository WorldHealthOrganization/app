import 'package:flutter/material.dart';


//todo add language changing functionality:
/*
 * Kurdish language is not supported by
 * in mobile phones like Arabic or Persian
 * we have to set the language manually
 */
class Settings extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: ThemeData.light().primaryColor,
      ),
      body: Column(
        children: <Widget>[],
      ),
    );
  }
}
